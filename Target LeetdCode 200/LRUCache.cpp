#include <bits/stdc++.h>
using namespace std;

class LRUCache {
   public:
    LRUCache(int capacity) {
        //  used as a con
    }

    int get(int key) {
    }

    void put(int key, int value) {
    }
};
