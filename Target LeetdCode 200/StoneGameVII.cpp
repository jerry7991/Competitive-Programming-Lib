#include <bits/stdc++.h>
using namespace std;

int stoneGameVII(vector<int>& stones) {
}

int main() {
    int n;
    cin >> n;
    vector<int> nums(n);
    for (int i = 0; i < n; i++) cin >> nums[i];
    cout << stoneGameVII(nums);
}