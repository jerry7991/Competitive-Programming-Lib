#include <bits/stdc++.h>
using namespace std;

vector<string> ambiguousCoordinates(string s) {
}

int main() {
    string str;
    cin >> str;
    vector<string> out = ambiguousCoordinates(str);
    for (string str : out) cout << str << " ";
}