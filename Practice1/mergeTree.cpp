#include<bits/stdc++.h>
#include "Tree.h"
using namespace std;


Tree<int> *mergeTrees(Tree<int> *root1, Tree<int>*root2){
	if(!root) return
}

int main() {
	Tree<int> *root = takeInput();
	root = mergeTrees(root);
	return 0;
}
